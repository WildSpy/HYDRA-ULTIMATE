# HYDRA Web Panel — зависимости.
#
# Сама панель написана на стандартной библиотеке Python 3.10+ (http.server, ssl,
# hashlib, hmac) и НЕ требует внешних пакетов. Ниже — опциональные зависимости,
# улучшающие функциональность (устанавливаются install-webpanel.sh best-effort):

qrcode        # генерация QR-кодов для клиентских ссылок (уже нужен основной платформе)
psutil        # точные метрики CPU/RAM/диска на дашборде (иначе — фолбэк на /proc)
